<div class="row">
  <div class="offset-2"></div>
  <div class="pod col-8 rounded shadow">
    <h1>Editing Department</h1>

    <div class="my-3">
      <%= render 'form', department: @department %>
    </div>

    <%= link_to 'Show', cms_department_path(@department), class: 'btn btn-sm btn-outline-primary' %>
    <%= link_to 'Back', cms_departments_path, class: 'btn btn-sm btn-outline-dark' %>
  </div>
</div>
